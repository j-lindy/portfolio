function animate(){requestAnimationFrame(animate),cube.rotation.x+=.01,cube.rotation.y+=.01,renderer.render(scene,camera)}const scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1e3),renderer=new THREE.WebGLRenderer;renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement);const geometry=new THREE.BoxGeometry,material=new THREE.MeshBasicMaterial({color:65280}),cube=new THREE.Mesh(geometry,material);scene.add(cube),camera.position.z=5,animate();